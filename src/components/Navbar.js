import { AppBar, Toolbar } from "@mui/material";

function Navbar() {
  return (
    <AppBar position="static">
      <Toolbar>
        <p>Small BiZZ</p>
      </Toolbar>
    </AppBar>
  );
}
export default Navbar;
